<nav class="navbar navbar-expand-lg navbar-light bg-info">
    <a class="navbar-brand text-white" href="/">SJA</a>
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
        <% if(!token || (token && url === '/')) { %>
        <a type="button" class="btn btn-outline-light btn-lg" id="login" href="/login">Login</a>
        <%} else {%>
          <a type="button" class="btn btn-outline-light btn-lg" id="logout">Logout</a>
        <% } %>
      </li>
    </ul>
</nav>

<script>
  const button = document.getElementById('logout');
    button.addEventListener('click', () => {
     window.location.pathname = '/logout'
    });
</script>
